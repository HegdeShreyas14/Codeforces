#include<bits/stdc++.h>
using namespace std;
using ll = long long;

int main(){
    int n,m;cin>>n>>m;
    const int MOD = 1e9 + 7;
    vector<int> a(n);
    for(auto&i : a) cin>>i;

    vector<vector<int>> dp(n,vector<int>(m+1,0));

    if(arr[0] == 0){
        for(int i = 1; i <= m; i++){
            dp[0][m] = 1;
        }
    }else{
        dp[]
    }



}
