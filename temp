#include <bits//stdc++.h>

using namespace std;

void solve() {
    int n;
    cin >> n;
    if(n < 10) cout << n << " 0" << endl;
    else if (n % 12 == 0) cout << "0 " << n << endl;
    

}
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}